from setuptools import setup, find_packages

setup(
    name ='mirage',
    version='0.1.0' ,
    py_modules=['script'],
    install_requires=[
        'Click'
    ],
    entry_points={

    
    }



)